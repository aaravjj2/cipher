# Loop 097 plan — README workflow line if labels changed

**Goal:** Update `cipher-system/README.md` Daily workflow only if a panel was renamed or the listed path is stale.

**Why this loop exists:** PROGRAM 097.

**Skip-if:** Daily workflow still names current panel labels (Morning Brief, Setup Scanner, Night Vision, Skew Map, Options Terminal, Journal, Paper Portfolios). 088/089 changed guest live-chain routing, not those labels.

**Files:** none.

**Preserve:** Paper-only, no browser order endpoint, guest catalog.

**Anti-goals:** Do not rewrite README as a guest tour. No commit.

**Checks:** none (docs skip).

## Result 2026-08-23

Skipped — panel labels in the Daily workflow line still match the catalog; no rename this program.
